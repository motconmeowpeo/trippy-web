import { environment } from "src/environments/environment";

export const API_UPLOAD =
  'https://ioypxsbwog.execute-api.ap-southeast-1.amazonaws.com/dev/trippy-storage/';

// URL Serve

export const API_ROLE = `${environment.apiUrl}role`;
export const API_USER = `${environment.apiUrl}user`;
export const API_TOUR = `${environment.apiUrl}tour`;
export const API_LOCATION =
  `${environment.apiUrl}location`;
export const API_COMMENT =
  `${environment.apiUrl}tour-comment`;
export const API_INVOICE =
  `${environment.apiUrl}invoice`;
export const API_CONTACT =
  `${environment.apiUrl}contact`;
