export enum STATUS {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}
export enum AUTH {
  VALID = 'VALID',
  INVALID = 'INVALID',
}

export enum ModalCloseStatus {
  COMPLETE = 'COMPLETE',
  CANCEL = 'CANCEL',
}

export enum InvoiceStatus {
  ACTIVE = 'ACTIVE',
  CANCELED = 'CANCELED',
  DONE = 'DONE',
}
