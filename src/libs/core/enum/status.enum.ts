export enum STATUS {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}
export enum AUTH {
  VALID = 'VALID',
  INVALID = 'INVALID',
}

export enum ModalCloseStatus {
  COMPLETE = 'COMPLETE',
  CANCEL = 'CANCEL',
}
